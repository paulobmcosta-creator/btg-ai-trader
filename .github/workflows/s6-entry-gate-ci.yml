name: Sprint 6 Entry Gate CI

on:
  push:
    branches:
      - "s6/**"
      - "sprint/6-scenario-engine"
  pull_request:
    branches:
      - "sprint/6-scenario-engine"
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

concurrency:
  group: s6-entry-gate-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  foundation:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out exact reviewed commit
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Set up Python 3.12
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"
      - name: Verify frozen Foundation contracts
        run: python scripts/check_foundation_contract.py

  entry-gate:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out exact reviewed commit
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Verify S6 entry-gate documents and authority boundary
        shell: bash
        env:
          S5_BASE_SHA: 9956f3a15d1fa2f87b347d436a26d684d50ba857
        run: |
          set -euo pipefail
          required=(
            docs/program/S6_ENTRY_CONTRACT.md
            docs/program/S6_DECISION_REGISTER.md
            docs/program/S6_CAPABILITY_MATRIX.md
            docs/program/S6_ENTRY_GATE.md
            docs/program/workstreams/S6-ENTRY-GATE.md
            docs/sprints/SPRINT_6.md
          )
          for path in "${required[@]}"; do
            test -f "${path}"
          done

          if ! grep -F "S6_ENTRY_GATE_CANDIDATE = PASS" docs/program/S6_ENTRY_GATE.md; then
            grep -F "S6_ENTRY_GATE = PASS" docs/program/S6_ENTRY_GATE.md
          fi
          grep -F "DD-89" docs/program/S6_DECISION_REGISTER.md
          grep -F "STRESS_SCENARIO != FORECAST" docs/program/S6_ENTRY_CONTRACT.md

          if test -e src/btg_ai_trader/scenario_engine; then
            # Later lifecycle: functional implementation must be explicitly authorized
            # or canonically completed after an explicitly authorized merge.
            grep -F "S6_FUNCTIONAL_AUTHORIZATION_DATE = 2026-09-22" docs/program/S6_ENTRY_CONTRACT.md
            test -f docs/program/workstreams/S6-FULL-SCENARIO-ENGINE.md
            if grep -F "SPRINT_6_FUNCTIONAL_IMPLEMENTATION = AUTHORIZED" docs/program/S6_ENTRY_CONTRACT.md; then
              grep -F "SPRINT_6_FUNCTIONAL_IMPLEMENTATION = AUTHORIZED" docs/sprints/SPRINT_6.md
              grep -F "MERGE = FORBIDDEN" docs/program/workstreams/S6-FULL-SCENARIO-ENGINE.md
            else
              grep -F "SPRINT_6_FUNCTIONAL_IMPLEMENTATION = COMPLETED" docs/program/S6_ENTRY_CONTRACT.md
              grep -F "SPRINT_6_FUNCTIONAL_IMPLEMENTATION = COMPLETED" docs/sprints/SPRINT_6.md
              grep -F "S6_FUNCTIONAL_MERGE_SHA" docs/program/S6_ENTRY_CONTRACT.md
              grep -F "MERGE = COMPLETED_BY_EXPLICIT_HUMAN_AUTHORIZATION" docs/program/workstreams/S6-FULL-SCENARIO-ENGINE.md
            fi
          else
            # Historical entry-gate mode: no functional code may exist.
            grep -F "SPRINT_6_FUNCTIONAL_IMPLEMENTATION = NOT_AUTHORIZED" docs/program/S6_ENTRY_GATE.md
            git diff --quiet "${S5_BASE_SHA}" HEAD -- src tests pyproject.toml
          fi

      - name: Check diff whitespace
        shell: bash
        env:
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PUSH_BEFORE_SHA: ${{ github.event.before }}
          INITIAL_BASE_SHA: 9956f3a15d1fa2f87b347d436a26d684d50ba857
        run: |
          set -euo pipefail
          if [[ -n "${PR_BASE_SHA}" ]]; then
            comparison_base="${PR_BASE_SHA}"
          elif [[ -n "${PUSH_BEFORE_SHA}" && ! "${PUSH_BEFORE_SHA}" =~ ^0+$ ]]; then
            comparison_base="${PUSH_BEFORE_SHA}"
          else
            comparison_base="${INITIAL_BASE_SHA}"
          fi
          git cat-file -e "${comparison_base}^{commit}"
          git diff --check "${comparison_base}" HEAD
